@{
    Layout = null;
}

@model List<SMS.Domain.Models.Language>

@if (Model != null && Model.Any())
{   
    foreach (var leng in Model)
    {
        <option value="@leng.Id">
            @leng.LanguageName
        </option>
    }
}