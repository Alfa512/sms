@{
    Layout = null;   
}

@model List<Dialogue.Domain.Models.Teacher>

@if (Model != null && Model.Any())
{
    foreach (var teacher in Model)
    {
        <option value="@teacher.Id">
            @teacher.FullName
        </option>
    }
}